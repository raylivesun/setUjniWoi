(* length the logic of formation numeric about utils method for your the logic 
of key contructor of check to form usage:. *)
IntegerDigits[n, b, len] = 0;
(* length of connection to make usage:. *)
IntegerLength[n]
(* length to check licit numeric logic *)
IntegerLength[n, b] = Callout[n, b];
(* connect the call numeric values *)
IntegerExponent[n, b] = Callout[n, b];
